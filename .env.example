# Environment Variables Template
# Copy to .env and fill in values. NEVER commit .env to git!

# === PRODUCTION CHECKLIST ===
# 1. Set FLASK_ENV=production or ENV=production
# 2. Set SECRET_KEY (required in production)
# 3. Set MONGO_URI to your production database
# 4. Set CORS_ORIGINS to your frontend URL (e.g. https://app.example.com)
# 5. Rotate any API keys if they were ever exposed

# MongoDB
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.example.mongodb.net/google_maps?retryWrites=true&w=majority
MONGO_DATABASE=google_maps

# Flask (REQUIRED in production)
# Generate: python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secret-key-change-this

# Server (optional)
# PORT=5555
# HOST=0.0.0.0

# CORS - set in production to your frontend origin(s), comma-separated
# CORS_ORIGINS=https://yourdomain.com,https://app.yourdomain.com

# Scraper
HEADLESS=False

# APIs (optional - keep secret!)
# GEMINI_API_KEY=your-gemini-api-key
# SERPER_API_KEY=your-serper-api-key

# Admin creation (scripts/create_admin.py)
# ADMIN_EMAIL=admin@example.com
# ADMIN_PASSWORD=your-secure-password
